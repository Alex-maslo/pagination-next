import React from "react";

const Pagination = () => {
  return (
    <div className="flex items-center justify-center">
      <div className="join">
        <button className="join-item btn btn-accent">«</button>
        <button className="join-item btn btn-accent">Page 22</button>
        <button className="join-item btn btn-accent">»</button>
      </div>
    </div>
  );
};

export default Pagination;
